git_url(ENV["GIT_REPO_CERT"])
git_basic_authorization(Base64.strict_encode64("#{ENV['MATCH_REPOSITORY_ACCOUNT']}:#{ENV['MATCH_PERSONAL_ACCESS_TOKEN']}"))
storage_mode("git")
type("appstore")
app_identifier([ENV["IOS_APP_ID"]])
username(ENV["APPLE_DEVELOPER_EMAIL"])
